import { StyleSheet } from "react-native";

export const PersonalInformationStyle = () =>
    StyleSheet.create({
        container: {
            flex: 1,
            marginBottom: 8,
        },
    });

export default PersonalInformationStyle;
