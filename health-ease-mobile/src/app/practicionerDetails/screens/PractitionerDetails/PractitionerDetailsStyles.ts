import { StyleSheet } from "react-native";

const PractitionerDetailsStyles = StyleSheet.create({
    container: {
        flexGrow: 1,
        padding: 20,
        display: "flex",
        flexDirection: "column",
        gap: 10,
    },
});

export default PractitionerDetailsStyles;
