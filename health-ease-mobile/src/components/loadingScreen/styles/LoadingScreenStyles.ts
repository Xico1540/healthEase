import { StyleSheet } from "react-native";

const CreateLoadingScreenStyles = () =>
    StyleSheet.create({
        container: {
            flex: 1,
            justifyContent: "center",
            alignItems: "center",
        },
    });

export default CreateLoadingScreenStyles;
